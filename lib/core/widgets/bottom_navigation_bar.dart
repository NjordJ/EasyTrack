import 'package:flutter/material.dart';

class CustomBottomNavigatiorBar extends StatelessWidget {
  const CustomBottomNavigatiorBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
